spring:
  #datasource:
    # MySQL SETUP
   # driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    # DB Source URL
   # url: jdbc:tc:mysql:8.0.30://motudy_test

  jpa:
    hibernate:
      ddl-auto: update

